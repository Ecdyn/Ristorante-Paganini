---
phase: 03-german-onepage-head-and-seo-skeleton
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - de/index.html
autonomous: false
requirements:
  - FOUND-01
  - LANG-01
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05

must_haves:
  truths:
    - "de/index.html is a valid HTML5 document with lang='de' on the html element"
    - "The head contains all meta tags, hreflang, canonical, OG, Twitter card, favicon links, JSON-LD, CSS, and JS references"
    - "The body contains header with working nav/hamburger/mobile-menu structure, main with 12 section elements, and footer"
    - "JSON-LD Restaurant + LocalBusiness block is syntactically valid with all required fields present"
    - "Hreflang block has all 4 tags (de, en, it, x-default) with absolute URLs"
    - "FAQPage JSON-LD is NOT present (deferred to Phase 4)"
    - "Exactly one H1 element exists in the document"
  artifacts:
    - path: "de/index.html"
      provides: "Complete German onepage HTML skeleton with head and body structure"
      contains: "lang=\"de\""
  key_links:
    - from: "de/index.html"
      to: "assets/css/main.css"
      via: "link rel=stylesheet with ../assets/css/main.css"
      pattern: "href=\"\\.\\./assets/css/main\\.css\""
    - from: "de/index.html"
      to: "assets/js/main.js"
      via: "script defer with ../assets/js/main.js"
      pattern: "src=\"\\.\\./assets/js/main\\.js\""
    - from: "de/index.html header"
      to: "assets/js/main.js"
      via: "JS hook IDs: site-header, menu-toggle, mobile-menu, .hero-sentinel"
      pattern: "id=\"site-header\""
---

<objective>
Create /de/index.html as a complete, valid HTML5 document with a fully wired head section (meta, hreflang, JSON-LD, canonical, OG, Twitter card, favicon, CSS, JS) and a structural body skeleton (header with nav, 12 empty sections with anchor IDs, footer) — ready for Phase 4 content population.

Purpose: Establish the SEO and structural foundation for the German onepage so that Phase 4 can focus purely on content without touching any head elements or document structure.

Output: de/index.html (single file)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-german-onepage-head-and-seo-skeleton/03-CONTEXT.md
@.planning/phases/03-german-onepage-head-and-seo-skeleton/03-UI-SPEC.md
@.planning/phases/03-german-onepage-head-and-seo-skeleton/03-RESEARCH.md

<!-- Prior phase summary needed for favicon link tags -->
@.planning/phases/02-js-and-asset-foundation/02-02-SUMMARY.md

<!-- Reference markup for header/nav/mobile-menu structure -->
@assets/css/test.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create de/index.html with complete head and body skeleton</name>
  <files>de/index.html</files>

  <read_first>
    assets/css/test.html
    .planning/phases/03-german-onepage-head-and-seo-skeleton/03-UI-SPEC.md
    .planning/phases/03-german-onepage-head-and-seo-skeleton/03-RESEARCH.md
    .planning/phases/02-js-and-asset-foundation/02-02-SUMMARY.md
    assets/js/main.js
  </read_first>

  <action>
Create the directory `de/` and write `de/index.html` with the following exact structure. This is an assembly task — all contracts are defined in the UI-SPEC and RESEARCH. Follow them verbatim.

**1. Document root:**
```html
<!DOCTYPE html>
<html lang="de">
```

**2. Head section — elements in this exact order:**

Element 1 — charset (must be first in head):
```html
<meta charset="UTF-8">
```

Element 2 — viewport:
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

Element 3 — title (per D-07):
```html
<title>Ristorante Paganini | Italienisches Restaurant Leipzig</title>
```

Element 4 — meta description (per D-08, warm/inviting tone):
```html
<meta name="description" content="Erleben Sie authentische italienische Küche im Herzen von Leipzig. Ristorante Paganini — familiengeführt, mit Leidenschaft für Qualität und Gastfreundschaft seit Generationen. Tisch reservieren: +49 341 XXXXXXXX">
```
Note: The +49 341 XXXXXXXX is a placeholder. Add comment: `<!-- EDIT: Replace XXXXXXXX with real phone number before launch -->`

Element 5 — canonical (per D-13):
```html
<!-- PLACEHOLDER: Replace example.com with real domain before launch -->
<link rel="canonical" href="https://www.example.com/de/">
```

Element 6 — hreflang block (per D-12, D-14, SEO-03). All 4 tags with absolute URLs:
```html
<!-- PLACEHOLDER: Replace example.com with real domain in all hreflang URLs before launch -->
<link rel="alternate" hreflang="de" href="https://www.example.com/de/">
<link rel="alternate" hreflang="en" href="https://www.example.com/en/">
<link rel="alternate" hreflang="it" href="https://www.example.com/it/">
<link rel="alternate" hreflang="x-default" href="https://www.example.com/de/">
```
The hreflang="de" href MUST exactly match the canonical href. x-default points to DE (primary market).

Element 7 — Open Graph tags (per D-09, D-10, SEO-02):
```html
<meta property="og:type" content="restaurant">
<meta property="og:locale" content="de_DE">
<meta property="og:site_name" content="Ristorante Paganini">
<meta property="og:title" content="Ristorante Paganini | Italienisches Restaurant Leipzig">
<meta property="og:description" content="Erleben Sie authentische italienische Küche im Herzen von Leipzig. Ristorante Paganini — familiengeführt, mit Leidenschaft für Qualität und Gastfreundschaft seit Generationen. Tisch reservieren: +49 341 XXXXXXXX">
<meta property="og:url" content="https://www.example.com/de/">
<meta property="og:image" content="https://www.example.com/assets/img/og-image.jpg">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<!-- PLACEHOLDER: Replace example.com with real domain before launch -->
<!-- PLACEHOLDER: Replace og-image.jpg with real photography before launch -->
```

Element 8 — Twitter card tags (per D-11, SEO-02):
```html
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Ristorante Paganini | Italienisches Restaurant Leipzig">
<meta name="twitter:description" content="Erleben Sie authentische italienische Küche im Herzen von Leipzig. Ristorante Paganini — familiengeführt, mit Leidenschaft für Qualität und Gastfreundschaft seit Generationen. Tisch reservieren: +49 341 XXXXXXXX">
<meta name="twitter:image" content="https://www.example.com/assets/img/og-image.jpg">
<!-- PLACEHOLDER: Replace example.com and og-image.jpg before launch -->
```

Element 9 — Favicon set (exact from 02-02-SUMMARY.md, using ../assets/ prefix):
```html
<!-- Favicon set — SEO-09 -->
<link rel="icon" href="../assets/favicon.svg" type="image/svg+xml">
<link rel="icon" href="../assets/favicon.ico" sizes="32x32">
<link rel="apple-touch-icon" href="../assets/apple-touch-icon.png">
<link rel="manifest" href="../assets/site.webmanifest">
```

Element 10 — Stylesheet:
```html
<link rel="stylesheet" href="../assets/css/main.css">
```

Element 11 — JSON-LD structured data (per D-03, D-04, D-06, SEO-04). Place inside `<script type="application/ld+json">`:
```json
{
  "@context": "https://schema.org",
  "@type": ["Restaurant", "LocalBusiness"],
  "name": "Ristorante Paganini",
  "description": "Authentisches italienisches Restaurant in der Leipziger Innenstadt. Familiengeführt seit Generationen.",
  "url": "https://www.example.com/de/",
  "telephone": "+49 341 XXXXXXXX",
  "servesCuisine": "Italian",
  "priceRange": "€€",
  "currenciesAccepted": "EUR",
  "paymentAccepted": "Cash, Credit Card",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Grosse Fleischergasse",
    "addressLocality": "Leipzig",
    "postalCode": "04109",
    "addressCountry": "DE"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": 51.3397,
    "longitude": 12.3731
  },
  "openingHoursSpecification": [
    {
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
      "opens": "11:30",
      "closes": "22:00"
    },
    {
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Saturday", "Sunday"],
      "opens": "12:00",
      "closes": "22:00"
    }
  ],
  "image": "https://www.example.com/assets/img/og-image.jpg",
  "inLanguage": "de"
}
```
After the closing `</script>` tag, add these comments:
```html
<!-- EDIT: telephone — replace XXXXXXXX with real number before launch -->
<!-- EDIT: openingHoursSpecification — confirm actual hours with owner before launch -->
<!-- PLACEHOLDER: Replace example.com and og-image.jpg with real values before launch -->
<!-- SYNC: Update this JSON-LD block in en/ and it/ when translating in Phase 5 -->
```

Element 12 — JavaScript (defer, non-blocking):
```html
<script src="../assets/js/main.js" defer></script>
```

Do NOT include: `<meta name="robots">` (omitting defaults to index,follow), Google Fonts CDN links, FAQPage JSON-LD (D-05 defers to Phase 4), any `@import` CSS.

**3. Body section:**

Adapt the header/nav/mobile-menu structure directly from `assets/css/test.html` (lines 32-68). The critical JS hook IDs that MUST be preserved exactly:
- `id="site-header"` on the `<header>` element
- `id="menu-toggle"` on the hamburger `<button>`
- `id="mobile-menu"` on the mobile overlay `<div>`
- `class="mobile-menu-close"` on the close button inside mobile-menu
- `class="hero-sentinel"` on the sentinel div inside #hero

Header structure (adapted from test.html with full German nav labels per D-01):
```html
<header class="header" id="site-header">
  <a href="#hero" class="font-heading" style="font-size: 1.5rem; text-decoration: none; color: var(--color-accent); font-weight: 700;">
    Ristorante Paganini
  </a>

  <nav>
    <ul class="nav" role="list">
      <li><a href="#hero">Start</a></li>
      <li><a href="#about">Über uns</a></li>
      <li><a href="#menu">Speisekarte</a></li>
      <li><a href="#lunch">Mittagstisch</a></li>
      <li><a href="#reservation">Reservierung</a></li>
      <li><a href="#contact">Kontakt</a></li>
    </ul>
  </nav>

  <button type="button" class="menu-toggle" id="menu-toggle"
          aria-expanded="false" aria-controls="mobile-menu"
          aria-label="Navigation öffnen">
    <span></span>
    <span></span>
    <span></span>
  </button>
</header>
```

Mobile menu overlay (adapted from test.html with all nav targets per D-01):
```html
<div class="mobile-menu" id="mobile-menu" role="dialog" aria-modal="true" aria-label="Navigation">
  <button type="button" class="mobile-menu-close" aria-label="Navigation schließen">Schließen</button>
  <!-- EDITABLE: Mobile nav links — update anchor text and add/remove links as needed -->
  <a href="#hero">Start</a>
  <a href="#info">Öffnungszeiten</a>
  <a href="#about">Über uns</a>
  <a href="#menu">Speisekarte</a>
  <a href="#lunch">Mittagstisch</a>
  <a href="#gallery">Galerie</a>
  <a href="#reservation">Reservierung</a>
  <a href="#events">Veranstaltungen</a>
  <a href="#location">Anfahrt</a>
  <a href="#faq">FAQ</a>
  <a href="#contact">Kontakt</a>
</div>
```

Note: Use proper German characters with umlauts (ö, ü, ß) — NOT ASCII substitutes. The test.html used "oeffnen" and "schliessen" but the real page must use "öffnen" and "schließen".

Main element with all 12 sections (per D-01, D-17):
```html
<main>
  <section class="hero" id="hero">
    <!-- SECT-01: Hero — content added in Phase 4 -->
    <!-- EDITABLE: Hero image, heading, subheadline, and CTA buttons added in Phase 4 -->
    <h1><!-- EDITABLE: Restaurant name heading — filled in Phase 4 --></h1>
    <div class="hero-sentinel" aria-hidden="true"></div>
  </section>

  <section id="info">
    <!-- SECT-02: Quick Info Bar — content added in Phase 4 -->
  </section>

  <section id="about">
    <!-- SECT-03: About / Philosophy — content added in Phase 4 -->
  </section>

  <section id="menu">
    <!-- SECT-04: Menu Overview — content added in Phase 4 -->
  </section>

  <section id="lunch">
    <!-- SECT-05: Business Lunch — content added in Phase 4 -->
  </section>

  <section id="gallery">
    <!-- SECT-06: Gallery — content added in Phase 4 -->
  </section>

  <section id="reservation">
    <!-- SECT-07: Reservation — content added in Phase 4 -->
  </section>

  <section id="events">
    <!-- SECT-08: Groups / Events — content added in Phase 4 -->
  </section>

  <section id="location">
    <!-- SECT-09: Location — content added in Phase 4 -->
  </section>

  <section id="faq">
    <!-- SECT-10: FAQ — content and FAQPage JSON-LD added in Phase 4 -->
  </section>

  <section id="contact">
    <!-- SECT-11: Contact — content added in Phase 4 -->
  </section>
</main>

<footer id="footer">
  <!-- SECT-12: Footer — content added in Phase 4 -->
</footer>
```

The `<h1>` is placed inside the hero section as the single H1 for the page (per FOUND-01). It contains a placeholder comment — Phase 4 fills it with the restaurant name. No other H1 exists anywhere in the document.

Close with `</body></html>`.
  </action>

  <verify>
    <automated>cd "C:/Users/PC/Documents/Ristorante Paganini" && test -f de/index.html && grep -q 'lang="de"' de/index.html && grep -c 'hreflang' de/index.html | grep -q '4' && grep -q 'application/ld+json' de/index.html && grep -q 'og:type.*restaurant' de/index.html && grep -q 'canonical' de/index.html && grep -q 'id="site-header"' de/index.html && grep -q 'id="menu-toggle"' de/index.html && grep -q 'id="mobile-menu"' de/index.html && grep -q 'hero-sentinel' de/index.html && echo "ALL CHECKS PASSED" || echo "CHECKS FAILED"</automated>
  </verify>

  <acceptance_criteria>
    - de/index.html exists and contains `lang="de"` on the html element
    - `grep -c 'hreflang' de/index.html` returns exactly 4
    - `grep -q 'application/ld+json' de/index.html` succeeds (JSON-LD block present)
    - `grep -q 'og:type" content="restaurant"' de/index.html` succeeds
    - `grep -q 'twitter:card" content="summary_large_image"' de/index.html` succeeds
    - `grep -q 'rel="canonical"' de/index.html` succeeds
    - `grep -q 'id="site-header"' de/index.html` succeeds (JS hook preserved)
    - `grep -q 'id="menu-toggle"' de/index.html` succeeds (JS hook preserved)
    - `grep -q 'id="mobile-menu"' de/index.html` succeeds (JS hook preserved)
    - `grep -q 'hero-sentinel' de/index.html` succeeds (JS hook preserved)
    - `grep -c 'id="hero"\|id="info"\|id="about"\|id="menu"\|id="lunch"\|id="gallery"\|id="reservation"\|id="events"\|id="location"\|id="faq"\|id="contact"\|id="footer"' de/index.html` returns 12 (all section IDs present)
    - `grep -c '<h1>' de/index.html` returns exactly 1 (single H1 rule)
    - `grep -q 'href="../assets/css/main.css"' de/index.html` succeeds (CSS linked)
    - `grep -q 'src="../assets/js/main.js"' de/index.html` succeeds (JS linked with defer)
    - `grep -q 'favicon.svg' de/index.html` succeeds (favicon set present)
    - `grep -q 'site.webmanifest' de/index.html` succeeds (webmanifest linked)
    - `grep -q 'FAQPage' de/index.html` fails (FAQPage JSON-LD NOT present per D-05)
    - `grep -q 'fonts.googleapis.com' de/index.html` fails (no Google Fonts CDN)
    - `grep -q 'PLACEHOLDER' de/index.html` succeeds (placeholder comments present)
    - `grep -q 'EDIT:' de/index.html` succeeds (edit comments present)
    - `grep -q 'SYNC:' de/index.html` succeeds (sync comments present)
    - `grep -q 'Ristorante Paganini | Italienisches Restaurant Leipzig' de/index.html` succeeds (exact title per D-07)
  </acceptance_criteria>

  <done>
    de/index.html exists as a valid HTML5 document with: lang="de", complete head (charset, viewport, title per D-07, meta description per D-08, canonical per D-13, 4 hreflang tags per D-12, OG tags per D-09/D-10, Twitter card per D-11, favicon set, main.css link, JSON-LD Restaurant+LocalBusiness per D-04, main.js defer), and body skeleton (header with nav matching test.html JS hooks, mobile menu overlay, main with 12 empty sections per D-01/D-17, single H1 per FOUND-01, hero-sentinel div, footer). No FAQPage JSON-LD (D-05). No Google Fonts CDN. All placeholder/edit/sync comments present.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify HTML structure and JSON-LD in browser</name>
  <files>de/index.html</files>
  <action>Human verifies the rendered page in browser and the JSON-LD via Google Rich Results Test. No code changes — this is a visual and functional verification checkpoint after Task 1 automated creation.</action>
  <what-built>Complete de/index.html with head (meta, hreflang, JSON-LD, OG, Twitter card, favicon, CSS, JS) and body skeleton (header/nav, 12 sections, footer). The page should render with the existing CSS design system and the sticky header / mobile menu JS should function.</what-built>
  <how-to-verify>
    1. Open de/index.html in a browser — verify the page loads without console errors (F12 > Console)
    2. Verify the sticky header appears and the hamburger menu button is visible on mobile width (resize to 375px or use DevTools mobile view)
    3. Click the hamburger — verify the mobile menu overlay opens with all nav links in German
    4. Press Escape or click "Schließen" — verify the overlay closes
    5. View Page Source (Ctrl+U) — confirm:
       a. `<meta charset="UTF-8">` is the FIRST element in head
       b. Title reads "Ristorante Paganini | Italienisches Restaurant Leipzig"
       c. 4 hreflang link tags are present (de, en, it, x-default)
       d. Canonical href matches the de hreflang href exactly
       e. og:type is "restaurant"
       f. JSON-LD block contains "@type": ["Restaurant", "LocalBusiness"]
       g. Exactly one `<h1>` exists in the entire document
    6. Copy the JSON-LD block content and paste into https://search.google.com/test/rich-results — verify zero errors (warnings about placeholder values are acceptable)
    7. Scroll through the page — verify 12 empty sections exist (they will be visually minimal since no content yet)
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint — see how-to-verify steps above"</automated>
  </verify>
  <done>Human confirms: page renders without console errors, mobile menu works, JSON-LD passes Rich Results Test, all head elements verified in page source.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static file serving | de/index.html served as static file — no server-side processing, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Placeholder values (example.com, XXXXXXXX) | accept | Placeholder domain and phone are non-sensitive; real values substituted before launch per PLACEHOLDER comments |
| T-03-02 | Spoofing | hreflang/canonical URLs use placeholder domain | accept | example.com is IANA-reserved; no spoofing risk until real domain is configured |
| T-03-03 | Information Disclosure | No Google Fonts CDN | mitigate | GDPR compliance enforced: zero external font requests. Verified by grep: `fonts.googleapis.com` must NOT appear in de/index.html |
| T-03-04 | Tampering | JSON-LD static content | accept | All JSON-LD values are hardcoded strings with no dynamic injection vector; static file has no server-side rendering |
</threat_model>

<verification>
Structural verification (automated):
- `test -f de/index.html` — file exists
- `grep -c 'hreflang' de/index.html` — returns 4
- `grep -c '<section' de/index.html` — returns 11 (11 sections in main; footer is not a section element)
- `grep -c '<h1>' de/index.html` — returns 1
- `grep -q 'application/ld+json' de/index.html` — JSON-LD present
- `grep -q 'fonts.googleapis.com' de/index.html` — must NOT match (GDPR)

Content verification (human checkpoint):
- Browser renders without console errors
- Mobile menu opens/closes correctly
- JSON-LD passes Google Rich Results Test
</verification>

<success_criteria>
- de/index.html is a valid HTML5 document with lang="de"
- Head contains all 12 required element groups in correct order
- Body contains header (with JS hooks), main (12 sections including hero through contact + footer), footer
- JSON-LD Restaurant + LocalBusiness passes Rich Results Test (zero errors)
- Hreflang cluster has 4 tags with absolute URLs, canonical matches self-hreflang
- Single H1 exists in document
- No Google Fonts CDN references
- All placeholder/edit/sync comments present for agency handoff
- Mobile menu JS functions correctly (hamburger opens overlay, Escape/close closes it)
</success_criteria>

<output>
After completion, create `.planning/phases/03-german-onepage-head-and-seo-skeleton/03-01-SUMMARY.md`
</output>
