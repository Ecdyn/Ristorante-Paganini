---
phase: 04-german-onepage-all-12-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/main.css
  - assets/pdf/speisekarte.pdf
  - assets/pdf/mittagskarte.pdf
autonomous: true
requirements:
  - SECT-04
  - SECT-05
  - SECT-06
  - PERF-02

must_haves:
  truths:
    - "Gallery CSS grid renders 3 columns on desktop with one image spanning 2 columns"
    - "Hover captions appear on gallery items with smooth opacity transition"
    - "Lightbox modal CSS provides dark overlay, centered image, and navigation controls"
    - "Menu category cards display in 4-column grid on desktop, 2-col tablet, 1-col mobile"
    - "Placeholder PDFs exist for menu and lunch download CTAs"
  artifacts:
    - path: "assets/css/main.css"
      provides: "15 new CSS block classes for Phase 4 sections"
      contains: ".gallery"
    - path: "assets/pdf/speisekarte.pdf"
      provides: "Placeholder PDF for menu download CTA"
    - path: "assets/pdf/mittagskarte.pdf"
      provides: "Placeholder PDF for lunch menu download CTA"
  key_links:
    - from: "assets/css/main.css"
      to: "de/index.html"
      via: "class names referenced in HTML markup"
      pattern: "\\.gallery|\\.menu-card|\\.lightbox|\\.info-bar|\\.faq-list|\\.lunch-highlights"
---

<objective>
Add all new CSS block classes needed for Phase 4 content sections and create placeholder PDF assets.

Purpose: Establish the CSS foundation and file assets that Plans 02 and 03 depend on, so content markup can reference these classes immediately.
Output: Extended main.css with 15 new blocks, two placeholder PDFs in assets/pdf/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-german-onepage-all-12-content-sections/04-CONTEXT.md
@.planning/phases/04-german-onepage-all-12-content-sections/04-UI-SPEC.md

<interfaces>
<!-- Existing CSS structure in main.css the executor must extend -->

From assets/css/main.css (structure):
- Line 369: `/* == 5. BLOCKS === */` section where new blocks go
- Line 640: `/* == 6. EXCEPTIONS === */` section (new blocks go BEFORE this)
- Existing blocks: .header, .nav, .menu-toggle, .mobile-menu, .hero, .hero__content, .hero__bg, .btn, .section, .section--alt, .section--dark

From assets/css/main.css (composition classes new blocks will use):
```css
.flow > * + * { margin-block-start: var(--flow-space, 1em); }
.cluster { display: flex; flex-wrap: wrap; gap: var(--cluster-gap, var(--space-4)); }
.wrapper { max-width: var(--wrapper-max); margin-inline: auto; padding-inline: var(--wrapper-padding); }
.content-wrapper { max-width: var(--content-max); margin-inline: auto; }
.grid { display: grid; gap: var(--grid-gap, var(--space-6)); }
.grid[data-layout="halves"] — 2-col at 48rem+
.grid[data-layout="thirds"] — 3-col at 64rem+
.grid[data-layout="quarters"] — 4-col at 64rem+
```

Design tokens (key ones for new blocks):
```css
--space-4: 1rem; --space-6: 1.5rem; --space-8: 2rem; --space-12: 3rem;
--color-accent: #7A2D3A; --color-surface: #FBF5F5; --color-surface-dark: #2C1A1D;
--color-text-inverse: #FBF5F5; --color-text-muted: #6B5557;
--shadow-sm: 0 1px 3px rgba(42, 31, 32, 0.08);
--radius-md: 8px;
--transition-base: 250ms ease;
--z-modal: 900;
--font-heading: 'Cormorant Garamond', Georgia, 'Times New Roman', serif;
--font-size-sm: 0.875rem;
--font-weight-normal: 400; --font-weight-bold: 700;
--section-spacing: clamp(5rem, 8vw, 7.5rem);
```

H3 rule (UI-SPEC override): H3 within new blocks must use `font-family: var(--font-heading); font-size: 1rem; font-weight: var(--font-weight-normal)` — NOT `--font-size-heading-sm` (22px).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all Phase 4 CSS blocks to main.css</name>
  <files>assets/css/main.css</files>
  <read_first>
    - assets/css/main.css (full file — executor must see existing structure, tokens, and blocks before appending)
    - .planning/phases/04-german-onepage-all-12-content-sections/04-UI-SPEC.md (section "New CSS Needed in main.css" — exact list of 15 blocks)
  </read_first>
  <action>
Append 15 new CSS block definitions to Section 5 (BLOCKS) of assets/css/main.css, BEFORE the `/* == 6. EXCEPTIONS */` comment (currently at line 640). Each block must use only CSS custom property tokens — never hard-coded hex/px values.

Add these blocks in order:

1. `.section-label` — brand mark above H1 in hero:
   `font-family: var(--font-body); font-size: var(--font-size-sm); text-transform: uppercase; letter-spacing: 0.2em; color: var(--color-text-inverse); font-weight: var(--font-weight-normal);`

2. `.info-bar` — quick info strip layout (per UI-SPEC #info):
   `display: flex; flex-wrap: wrap; justify-content: center; align-items: center; gap: var(--space-6); padding: var(--space-8) var(--wrapper-padding); font-family: var(--font-body); font-size: var(--font-size-sm);`
   Mobile override at max-width 47.999rem: `display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-4); text-align: center;`

3. `.menu-cards` — 4-column grid for menu category cards:
   Extends `.grid` pattern. Default 1-col. At 48rem+: `grid-template-columns: repeat(2, 1fr)`. At 64rem+: `grid-template-columns: repeat(4, 1fr)`.

4. `.menu-card` — individual category card:
   `background-color: var(--color-surface); box-shadow: var(--shadow-sm); border-radius: var(--radius-md); padding: var(--space-6);`
   H3 inside: `font-family: var(--font-heading); font-size: 1rem; font-weight: var(--font-weight-normal); margin-block-end: var(--space-4);`

5. `.menu-card__dish` — dish name in card: `font-weight: var(--font-weight-bold);`

6. `.menu-card__desc` — dish description: `font-size: var(--font-size-sm); color: var(--color-text-muted);`

7. `.lunch-highlights` — flow list for monthly-editable lunch slots:
   `display: flex; flex-direction: column; gap: var(--space-6);`

8. `.lunch-slot` — individual lunch highlight slot:
   `border-left: 2px solid var(--color-accent); padding-left: var(--space-4);`

9. `.gallery` — CSS grid container:
   `display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-6);`
   Mobile at max-width 47.999rem: `grid-template-columns: 1fr;`
   Tablet at min-width 48rem and max-width 63.999rem: `grid-template-columns: repeat(2, 1fr);`

10. `.gallery__item` — grid item wrapper:
    `position: relative; overflow: hidden; border-radius: var(--radius-md); cursor: pointer;`

11. `.gallery__item[data-span="2"]` — 2-column span image:
    `grid-column: span 2;`
    Mobile override at max-width 47.999rem: `grid-column: span 1;`

12. `.gallery__caption` — hover caption overlay:
    `position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; background: rgba(28, 12, 14, 0.8); color: var(--color-text-inverse); font-size: var(--font-size-sm); font-family: var(--font-body); opacity: 0; transition: opacity var(--transition-base); padding: var(--space-4);`
    Show on hover/focus: `.gallery__item:hover .gallery__caption, .gallery__item:focus-within .gallery__caption { opacity: 1; }`

13. `.lightbox` — fixed modal overlay:
    `position: fixed; inset: 0; z-index: var(--z-modal); background: rgba(44, 26, 29, 0.95); display: flex; align-items: center; justify-content: center; visibility: hidden; opacity: 0; transition: opacity var(--transition-base), visibility var(--transition-base);`
    Open state: `.lightbox[data-open="true"] { visibility: visible; opacity: 1; }`

14. `.lightbox__img` — image inside modal:
    `max-width: 90vw; max-height: 85vh; object-fit: contain; border-radius: var(--radius-md);`

15. `.lightbox__nav` — navigation button positioning:
    `position: absolute; top: 50%; transform: translateY(-50%);`
    `.lightbox__nav[data-dir="prev"] { left: var(--space-4); }`
    `.lightbox__nav[data-dir="next"] { right: var(--space-4); }`
    `.lightbox__close` — close button: `position: absolute; top: var(--space-4); right: var(--space-4);`

16. `.faq-list` — FAQ dl/details list spacing:
    `display: flex; flex-direction: column; gap: var(--space-6);`
    `.faq-list dt, .faq-list summary { font-family: var(--font-heading); font-size: 1rem; font-weight: var(--font-weight-normal); cursor: pointer; }`
    `.faq-list dd { color: var(--color-text); margin-inline-start: 0; margin-block-start: var(--space-2); padding-block-end: var(--space-4); border-bottom: 1px solid var(--color-border); }`

Ensure `@media (prefers-reduced-motion: reduce)` already exists in global reset (it does at line 262) and covers transitions in new blocks.

IMPORTANT: Do NOT modify any existing CSS rules. Only append new blocks.
  </action>
  <verify>
    <automated>node -e "const css=require('fs').readFileSync('assets/css/main.css','utf8'); const blocks=['.gallery','.gallery__item','.gallery__caption','.lightbox','.lightbox\\\\[data-open','.menu-cards','.menu-card','.lunch-highlights','.info-bar','.faq-list','.section-label','.lightbox__img','.lightbox__nav','.lunch-slot','.lightbox__close']; const found=blocks.filter(b=>css.includes(b)); console.log('Found',found.length,'of',blocks.length,'blocks'); process.exit(found.length>=14?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - assets/css/main.css contains `.gallery` with `grid-template-columns: repeat(3, 1fr)`
    - assets/css/main.css contains `.gallery__caption` with `opacity: 0`
    - assets/css/main.css contains `.gallery__item:hover .gallery__caption` with `opacity: 1`
    - assets/css/main.css contains `.lightbox` with `z-index: var(--z-modal)`
    - assets/css/main.css contains `.lightbox[data-open="true"]` with `visibility: visible`
    - assets/css/main.css contains `.menu-cards` with responsive breakpoints at 48rem and 64rem
    - assets/css/main.css contains `.menu-card` with `box-shadow: var(--shadow-sm)`
    - assets/css/main.css contains `.info-bar` with `display: flex`
    - assets/css/main.css contains `.faq-list dt` or `.faq-list summary` with `font-family: var(--font-heading)`
    - assets/css/main.css contains `.section-label` with `letter-spacing: 0.2em`
    - No hard-coded hex values in new blocks (all reference CSS custom properties)
    - New blocks appear BEFORE the `/* == 6. EXCEPTIONS */` comment
    - H3 styling in .menu-card uses `font-size: 1rem` NOT `--font-size-heading-sm`
  </acceptance_criteria>
  <done>All 15+ new CSS blocks exist in main.css Section 5 (BLOCKS), using only design tokens, with responsive breakpoints for gallery and menu-cards, and hover/focus states for gallery captions and lightbox visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder PDF files for menu download CTAs</name>
  <files>assets/pdf/speisekarte.pdf, assets/pdf/mittagskarte.pdf</files>
  <read_first>
    - .planning/phases/04-german-onepage-all-12-content-sections/04-CONTEXT.md (D-08 and D-10 for PDF CTA context)
  </read_first>
  <action>
Create the `assets/pdf/` directory and two minimal placeholder PDF files:

1. `assets/pdf/speisekarte.pdf` — A minimal valid PDF containing text "Speisekarte — Ristorante Paganini" and "PLACEHOLDER: Replace with real menu PDF before launch". Generate using a minimal PDF string written directly (a valid PDF is just a text file with specific structure). Use Node.js to write a minimal valid PDF:

```javascript
const fs = require('fs');
const pdf = `%PDF-1.4
1 0 obj << /Type /Catalog /Pages 2 0 R >> endobj
2 0 obj << /Type /Pages /Kids [3 0 R] /Count 1 >> endobj
3 0 obj << /Type /Page /Parent 2 0 R /MediaBox [0 0 612 792] /Contents 4 0 R /Resources << /Font << /F1 5 0 R >> >> >> endobj
5 0 obj << /Type /Font /Subtype /Type1 /BaseFont /Helvetica >> endobj
4 0 obj << /Length 164 >>
stream
BT /F1 24 Tf 72 700 Td (Speisekarte) Tj ET
BT /F1 16 Tf 72 660 Td (Ristorante Paganini) Tj ET
BT /F1 12 Tf 72 600 Td (PLACEHOLDER: Replace with real menu PDF before launch) Tj ET
endstream
endobj
xref
0 6
0000000000 65535 f 
0000000009 00000 n 
0000000058 00000 n 
0000000115 00000 n 
0000000377 00000 n 
0000000266 00000 n 
trailer << /Size 6 /Root 1 0 R >>
startxref
593
%%EOF`;
fs.mkdirSync('assets/pdf', { recursive: true });
fs.writeFileSync('assets/pdf/speisekarte.pdf', pdf);
```

2. `assets/pdf/mittagskarte.pdf` — Same structure but with "Mittagskarte" instead of "Speisekarte" as the title text. Adjust the stream content accordingly.

Both files must be valid enough that a browser can open them without error. The exact byte offsets in the xref table must be correct for the PDF to be valid — if manual calculation is error-prone, use a simpler approach: create a minimal 1-page PDF via any available method (even a tiny binary).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const a=fs.existsSync('assets/pdf/speisekarte.pdf'); const b=fs.existsSync('assets/pdf/mittagskarte.pdf'); console.log('speisekarte.pdf:',a,'mittagskarte.pdf:',b); process.exit(a&&b?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - File `assets/pdf/speisekarte.pdf` exists and is non-empty
    - File `assets/pdf/mittagskarte.pdf` exists and is non-empty
    - Both files contain `%PDF` header (valid PDF marker)
    - Directory `assets/pdf/` exists
  </acceptance_criteria>
  <done>Two placeholder PDF files exist at assets/pdf/speisekarte.pdf and assets/pdf/mittagskarte.pdf, ready for download CTAs in the HTML content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file inclusion | main.css loaded by HTML pages — no user input, static file |
| PDF downloads | Static files served to browser — no dynamic content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | assets/css/main.css | accept | Static file on web server; no user input modifies CSS; standard file permissions suffice |
| T-04-02 | Information Disclosure | assets/pdf/*.pdf | accept | Placeholder content only; no sensitive data; real PDFs added before launch |
</threat_model>

<verification>
- All 15+ new CSS block classes exist in main.css
- No existing CSS rules modified
- Both placeholder PDFs exist and are valid
- New CSS blocks use only design token references (no hard-coded values)
</verification>

<success_criteria>
- main.css extended with gallery, lightbox, menu-card, info-bar, faq-list, lunch-highlights, and section-label blocks
- Responsive breakpoints present for gallery (1/2/3 col) and menu-cards (1/2/4 col)
- Hover caption and lightbox visibility states implemented via data-attributes
- Two placeholder PDFs ready for download CTAs
</success_criteria>

<output>
After completion, create `.planning/phases/04-german-onepage-all-12-content-sections/04-01-SUMMARY.md`
</output>
