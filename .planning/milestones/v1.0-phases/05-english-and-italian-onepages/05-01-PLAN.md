---
phase: 05-english-and-italian-onepages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - en/index.html
autonomous: true
requirements:
  - LANG-02
user_setup: []

must_haves:
  truths:
    - "English visitor sees fully localized content with no German strings on en/index.html"
    - "en/index.html has self-canonical pointing to /en/ URL"
    - "en/index.html has all 4 hreflang tags (de, en, it, x-default)"
    - "og:locale is en_GB and all OG/Twitter meta is in English"
    - "JSON-LD Restaurant description and inLanguage are in English"
    - "FAQPage JSON-LD has all 7 Q&A pairs translated to English matching visible FAQ text"
    - "Footer language switcher has aria-current on EN link"
    - "All aria-label attributes are translated to English"
  artifacts:
    - path: "en/index.html"
      provides: "Complete English localized onepage"
      min_lines: 700
  key_links:
    - from: "en/index.html"
      to: "../assets/css/main.css"
      via: "link rel stylesheet"
      pattern: "assets/css/main\\.css"
    - from: "en/index.html"
      to: "../assets/js/main.js"
      via: "script defer"
      pattern: "assets/js/main\\.js"
---

<objective>
Create the complete English localized onepage at /en/index.html by copying de/index.html and systematically replacing every German string with the English equivalent per the translation reference in 05-RESEARCH.md.

Purpose: Delivers requirement LANG-02 (English version fully localized) and completes one of the three files needed for hreflang reciprocal cluster.
Output: en/index.html — a fully functional English restaurant page sharing all CSS/JS/image assets with the German version.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-english-and-italian-onepages/05-CONTEXT.md
@.planning/phases/05-english-and-italian-onepages/05-RESEARCH.md
@de/index.html

<interfaces>
<!-- Source file: de/index.html — executor copies this and translates all German strings -->
<!-- Shared assets linked via relative paths (../assets/) — NO changes to paths needed -->
<!-- Section IDs are language-neutral: #hero, #info, #about, #menu, #lunch, #gallery, #reservation, #events, #location, #faq, #contact, #footer — do NOT translate -->
<!-- JS hooks: id="site-header", id="menu-toggle", id="mobile-menu" — do NOT translate -->
<!-- Class names and data attributes — do NOT translate -->
<!-- Italian dish names (Tagliatelle al Tartufo, etc.) — do NOT translate, only translate description text -->
<!-- H1 tagline "Dove il gusto incontra la tradizione" — do NOT translate (brand signature, D-10) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create en/index.html with full English localization</name>
  <files>en/index.html</files>
  <read_first>
    - de/index.html (source template — read entire file to understand structure)
    - .planning/phases/05-english-and-italian-onepages/05-RESEARCH.md (complete translation reference for every string)
    - .planning/phases/05-english-and-italian-onepages/05-CONTEXT.md (locked decisions D-01 through D-16)
  </read_first>
  <action>
Create directory `en/` if it does not exist. Copy `de/index.html` to `en/index.html`. Then systematically replace every German string with the English equivalent. Work through the file in this exact order:

**1. HTML element:**
- Change `lang="de"` to `lang="en"` on the `<html>` tag

**2. Head metadata (per D-06, D-07, D-08, D-15):**
- `<title>`: `Ristorante Paganini | Italian Restaurant Leipzig`
- `<meta name="description">`: `Experience authentic Italian cuisine in the heart of Leipzig. Ristorante Paganini — family-run, with a passion for quality and hospitality for generations. Reserve a table: +49 341 XXXXXXXX`
- `<link rel="canonical">`: change `/de/` to `/en/` in the href
- `og:locale`: change `de_DE` to `en_GB`
- `og:title`: `Ristorante Paganini | Italian Restaurant Leipzig`
- `og:description`: same as meta description above
- `og:url`: change `/de/` to `/en/`
- `twitter:title`: `Ristorante Paganini | Italian Restaurant Leipzig`
- `twitter:description`: same as meta description above
- hreflang block: keep ALL 4 tags UNCHANGED (they already point to de, en, it, x-default correctly)

**3. JSON-LD Restaurant/LocalBusiness block (per D-09):**
- `"description"`: `"Authentic Italian restaurant in central Leipzig. Family-run for generations."`
- `"inLanguage"`: `"en"`
- `"url"`: change `/de/` to `/en/`
- All other fields (name, telephone, address, geo, openingHoursSpecification, servesCuisine, priceRange, image) stay IDENTICAL

**4. JSON-LD FAQPage block (per D-04, D-05):**
Replace all 7 Q&A pairs with English translations. Each `"name"` and `"acceptedAnswer"."text"` must be translated. Use the exact text from the 05-RESEARCH.md "JSON-LD FAQPage block" section:
- FAQ 1: "Do I need a reservation?" / "We recommend a reservation, especially on weekends and public holidays..."
- FAQ 2: "Do you have a business lunch?" / "Yes, we offer a changing business lunch menu Monday to Friday from 11:30 to 14:30..."
- FAQ 3: "Where can I park?" / "Parking is available close by on Große Fleischergasse..."
- FAQ 4: "Do you have vegetarian or vegan dishes?" / "Of course. Our menu offers a selection of vegetarian dishes..."
- FAQ 5: "Do you have a terrace?" / "Yes, our restaurant has a welcoming terrace..."
- FAQ 6: "What payment methods do you accept?" / "We accept cash and all major credit and debit cards..."
- FAQ 7: "Are dogs welcome?" / "Yes, well-behaved dogs are very welcome..."

**5. Header navigation (desktop nav):**
- `Start` → `Home`
- `Über uns` → `About Us`
- `Speisekarte` → `Menu`
- `Mittagstisch` → `Business Lunch`
- `Reservierung` → `Reservations`
- `Kontakt` → `Contact`

**6. Mobile menu:**
- `aria-label="Navigation öffnen"` → `aria-label="Open navigation"`
- `aria-label="Navigation schließen"` → `aria-label="Close navigation"`
- `Schließen` (close button text) → `Close`
- `aria-label="Navigation"` on mobile menu → `aria-label="Navigation"` (same in EN)
- All mobile nav link texts: same translations as desktop nav, plus:
  - `Öffnungszeiten` → `Opening Hours`
  - `Galerie` → `Gallery`
  - `Veranstaltungen` → `Events`
  - `Anfahrt` → `Getting Here`
  - `FAQ` → `FAQ` (unchanged)

**7. Body sections — translate all visible text per 05-RESEARCH.md translation reference:**

Section by section:
- **Hero (#hero):** Keep H1 "Dove il gusto incontra la tradizione" UNCHANGED (D-10). Keep "Ristorante Paganini" UNCHANGED (D-11). Translate hero img alt, trust cues, CTAs per research reference.
- **Info bar (#info):** Translate labels (Öffnungszeiten→Opening Hours, Adresse→Address, Telefon→Phone, Mittagstisch→Business Lunch), day abbreviations (Mo→Mon, Fr→Fri, Sa→Sat, So→Sun), CTA, terrace note.
- **About (#about):** H2 "Our Story", translate all 3 paragraphs per research reference.
- **Menu (#menu):** H2 "Our Menu", translate intro para, category heading "Wein"→"Wine", translate dish descriptions ONLY (NOT dish names), CTAs "Download Menu" and "Full Menu". Keep PDF href unchanged.
- **Business Lunch (#lunch):** H2 "Business Lunch", translate intro, category names (Vorspeise→Starter, Hauptgericht→Main Course), dish descriptions, holiday note, CTA "Download Lunch Menu". Keep PDF href unchanged.
- **Gallery (#gallery):** H2 "Gallery", translate all 6 image alt texts and captions per research reference.
- **Reservation (#reservation):** H2 "Reserve a Table", translate intro, widget placeholder text, phone CTA "Call Us".
- **Events (#events):** H2 "Groups &amp; Events", translate both paragraphs, CTAs "Call Now" and "Send an Email".
- **Location (#location):** H2 "Getting Here", "Deutschland"→"Germany", translate parking label and items, map CTA "View on Google Maps", map image alt.
- **FAQ (#faq):** H2 "Frequently Asked Questions", translate all 7 dt/dd pairs — text MUST exactly match the FAQPage JSON-LD in the head.
- **Contact (#contact):** H2 "Contact", sub-heading "Get in Touch", labels (Telefon→Phone, E-Mail→Email, Adresse→Address), "Öffnungszeiten"→"Opening Hours", day names (Montag–Freitag→Monday–Friday, Samstag→Saturday, Sonntag→Sunday), Instagram "Follow us on Instagram".

**8. Lightbox modal (after main):**
- `aria-label="Bildergalerie"` → `aria-label="Image gallery"`
- Close button `aria-label="Galerie schließen"` → `aria-label="Close gallery"`
- Prev button `aria-label="Vorheriges Bild"` → `aria-label="Previous image"`
- Next button `aria-label="Nächstes Bild"` → `aria-label="Next image"`

**9. Footer (#footer):**
- `Rechtliches` → `Legal`
- `Impressum` → `Imprint` (link text), href stays `/en/impressum.html`
- `Datenschutz` → `Privacy Policy` (link text), href changes to `/en/datenschutz.html`
- Legal link hrefs: change `/de/impressum.html` → `/en/impressum.html`, `/de/datenschutz.html` → `/en/datenschutz.html`
- `Sprache` → `Language`
- Language switcher: move `aria-current="true"` and active styling from DE link to EN link. DE and IT links get plain `color: var(--color-text-inverse)` style. EN link gets `aria-current="true" style="color: var(--color-accent-light); font-weight: var(--font-weight-bold);"`
- Copyright: `Alle Rechte vorbehalten.` → `All rights reserved.`

**10. DO NOT change:**
- Any section `id` attribute (they are language-neutral)
- Any CSS class name or data attribute
- Any image `src`, `srcset`, `width`, `height` path
- Any `../assets/` relative path
- The `<!-- EDIT: -->`, `<!-- PLACEHOLDER: -->`, `<!-- SYNC: -->` comment markers (keep in English)
- The H1 Italian tagline
- Italian dish names in menu cards
- PDF file hrefs

**Tone per D-01:** Warm and professional. Same family "we" voice as German. Slightly less formal since English has no Sie/Du distinction. Polished but inviting.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Documents/Ristorante Paganini" && test -f en/index.html && echo "FILE EXISTS" && grep -c 'lang="en"' en/index.html && grep -c 'hreflang="de"' en/index.html && grep -c 'hreflang="en"' en/index.html && grep -c 'hreflang="it"' en/index.html && grep -c 'og:locale.*en_GB' en/index.html && grep -c 'Dove il gusto' en/index.html && echo "--- German string check (expect 0) ---" && grep -c 'Italienisches Restaurant' en/index.html || true && grep -c 'Über uns' en/index.html || true && grep -c 'Speisekarte' en/index.html || true && grep -c 'Alle Rechte vorbehalten' en/index.html || true</automated>
  </verify>
  <acceptance_criteria>
    - en/index.html exists and is >700 lines
    - `<html lang="en">` appears exactly once
    - `<title>` contains "Italian Restaurant Leipzig" (not "Italienisches")
    - `<link rel="canonical" href="https://www.example.com/en/">` present
    - All 4 hreflang tags present (de, en, it, x-default)
    - `og:locale` content is `en_GB`
    - `og:url` contains `/en/`
    - JSON-LD contains `"inLanguage": "en"`
    - H1 contains "Dove il gusto incontra la tradizione" (unchanged)
    - Zero occurrences of "Italienisches Restaurant" in the file
    - Zero occurrences of "Über uns" in the file
    - Zero occurrences of "Speisekarte" in the file (except possibly in comments referencing the PDF filename)
    - Zero occurrences of "Alle Rechte vorbehalten" in the file
    - Zero occurrences of "Öffnungszeiten" in the file
    - Footer contains `aria-current="true"` on the EN link
    - Footer legal links point to `/en/impressum.html` and `/en/datenschutz.html`
    - FAQ section contains "Do I need a reservation?" (first FAQ question in English)
    - All 6 gallery image alt texts are in English
    - Section IDs (#hero, #info, #about, etc.) unchanged from DE source
    - All `../assets/` paths unchanged from DE source
  </acceptance_criteria>
  <done>en/index.html is a complete, fully-localized English restaurant page with zero German strings in visible content, correct self-canonical, correct hreflang cluster, English OG/Twitter meta, English JSON-LD, English FAQPage schema matching visible FAQ text, and correct footer language switcher with EN active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML output | No user input processed; static content only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | en/index.html meta tags | accept | No PII in meta tags; phone number is intentionally public business info. Placeholder domain (example.com) prevents accidental exposure before launch. |
| T-05-02 | Tampering | hreflang cluster | accept | Static files served from web server; no dynamic content to tamper with. Hreflang integrity verified by cross-page grep checks. |
</threat_model>

<verification>
1. `test -f en/index.html` returns success
2. `grep -c 'lang="de"' en/index.html` returns 0 (no German lang attribute)
3. `grep -c 'lang="en"' en/index.html` returns at least 1
4. `grep -c 'hreflang=' en/index.html` returns 4 (de, en, it, x-default)
5. `grep -c 'canonical.*\/en\/' en/index.html` returns 1
6. `grep -c 'en_GB' en/index.html` returns 1
7. `grep 'Dove il gusto' en/index.html` returns the H1 line (brand signature preserved)
8. No German UI strings remain (spot-check: Über uns, Speisekarte, Öffnungszeiten, Reservierung, Kontakt)
</verification>

<success_criteria>
en/index.html exists as a complete English localized page with:
- Correct `lang="en"`, self-canonical to /en/, og:locale en_GB
- Complete hreflang reciprocal cluster (4 tags)
- All 12 sections translated with warm professional tone (D-01)
- FAQPage JSON-LD matches visible FAQ text
- Footer language switcher marks EN as active
- Zero German strings in visible content or meta tags
- All shared asset paths (CSS, JS, images, fonts) work correctly via ../assets/
</success_criteria>

<output>
After completion, create `.planning/phases/05-english-and-italian-onepages/05-01-SUMMARY.md`
</output>
