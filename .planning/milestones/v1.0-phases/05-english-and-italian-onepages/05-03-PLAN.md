---
phase: 05-english-and-italian-onepages
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - de/index.html
autonomous: false
requirements:
  - LANG-02
  - LANG-03

must_haves:
  truths:
    - "All three pages (de, en, it) have identical hreflang blocks with all 4 tags"
    - "Each page has self-canonical pointing to its own language URL"
    - "Language switcher in DE footer links to /en/ and /it/ correctly"
    - "Language switcher in EN footer links to /de/ and /it/ correctly"
    - "Language switcher in IT footer links to /de/ and /en/ correctly"
    - "No German strings remain in en/index.html or it/index.html"
    - "No English strings leaked into it/index.html and vice versa"
  artifacts:
    - path: "de/index.html"
      provides: "Verified DE page with correct hreflang and footer"
    - path: "en/index.html"
      provides: "Verified EN page"
    - path: "it/index.html"
      provides: "Verified IT page"
  key_links:
    - from: "de/index.html footer"
      to: "/en/ and /it/"
      via: "language switcher anchor tags"
      pattern: 'href="/en/".*href="/it/"'
    - from: "en/index.html footer"
      to: "/de/ and /it/"
      via: "language switcher anchor tags"
      pattern: 'href="/de/".*href="/it/"'
    - from: "it/index.html footer"
      to: "/de/ and /en/"
      via: "language switcher anchor tags"
      pattern: 'href="/de/".*href="/en/"'
---

<objective>
Verify hreflang reciprocal integrity across all three language files, confirm the DE page footer is correct, and run a comprehensive cross-validation to ensure zero German remnants in EN/IT pages and zero cross-language contamination.

Purpose: Guarantees the hreflang cluster is complete and reciprocal (the most common hreflang error is missing return tags), validates language switcher navigation, and catches any translation oversights.
Output: All three language files verified as a complete, correct multilingual cluster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-english-and-italian-onepages/05-CONTEXT.md
@de/index.html
@en/index.html
@it/index.html

@.planning/phases/05-english-and-italian-onepages/05-01-SUMMARY.md
@.planning/phases/05-english-and-italian-onepages/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify DE page hreflang and fix footer if needed</name>
  <files>de/index.html</files>
  <read_first>
    - de/index.html (verify hreflang block has all 4 tags, verify footer language switcher links)
    - .planning/phases/05-english-and-italian-onepages/05-CONTEXT.md (D-14 hreflang requirements, D-16 language switcher requirements)
  </read_first>
  <action>
Read de/index.html and verify the following are already correct (they should be from Phase 3):

1. **Hreflang block** contains all 4 tags:
   - `<link rel="alternate" hreflang="de" href="https://www.example.com/de/">`
   - `<link rel="alternate" hreflang="en" href="https://www.example.com/en/">`
   - `<link rel="alternate" hreflang="it" href="https://www.example.com/it/">`
   - `<link rel="alternate" hreflang="x-default" href="https://www.example.com/de/">`

2. **Self-canonical** points to DE: `<link rel="canonical" href="https://www.example.com/de/">`

3. **Footer language switcher** has:
   - DE link: `<a href="/de/" aria-current="true" style="color: var(--color-accent-light); font-weight: var(--font-weight-bold);">DE</a>`
   - EN link: `<a href="/en/" style="color: var(--color-text-inverse);">EN</a>`
   - IT link: `<a href="/it/" style="color: var(--color-text-inverse);">IT</a>`

If any of these are incorrect or missing, fix them. The DE page footer should already have correct language switcher links from Phase 3/4 — this task confirms and fixes only if needed.

Also verify footer legal links point to `/de/impressum.html` and `/de/datenschutz.html` (should already be correct).
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Documents/Ristorante Paganini" && echo "=== DE hreflang ===" && grep -c 'hreflang="de"' de/index.html && grep -c 'hreflang="en"' de/index.html && grep -c 'hreflang="it"' de/index.html && grep -c 'hreflang="x-default"' de/index.html && echo "=== DE canonical ===" && grep 'canonical' de/index.html && echo "=== DE footer switcher ===" && grep 'aria-current' de/index.html</automated>
  </verify>
  <acceptance_criteria>
    - de/index.html contains exactly 4 hreflang link tags (de, en, it, x-default)
    - de/index.html canonical href contains `/de/`
    - de/index.html footer has `aria-current="true"` on the DE language link
    - de/index.html footer has links to `/en/` and `/it/`
    - de/index.html footer legal links point to `/de/impressum.html` and `/de/datenschutz.html`
  </acceptance_criteria>
  <done>DE page has verified correct hreflang cluster with all return tags and correct footer language switcher.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-validate all three pages for hreflang reciprocity and translation completeness</name>
  <files>en/index.html, it/index.html</files>
  <read_first>
    - en/index.html (scan for any remaining German strings, verify hreflang and canonical)
    - it/index.html (scan for any remaining German strings or English leaks, verify hreflang and canonical)
    - de/index.html (reference for structural comparison)
  </read_first>
  <action>
Run a comprehensive cross-validation across all three files:

**1. Hreflang reciprocity check — each page must have all 4 tags:**
For each of de/index.html, en/index.html, it/index.html:
- Confirm `hreflang="de"` tag exists with href to `/de/`
- Confirm `hreflang="en"` tag exists with href to `/en/`
- Confirm `hreflang="it"` tag exists with href to `/it/`
- Confirm `hreflang="x-default"` tag exists with href to `/de/`

**2. Self-canonical check:**
- de/index.html: canonical href contains `/de/`
- en/index.html: canonical href contains `/en/`
- it/index.html: canonical href contains `/it/`

**3. German remnant check on EN page — grep for these German strings (all should return 0 matches):**
`Italienisches`, `Über uns`, `Speisekarte`, `Mittagstisch`, `Reservierung`, `Veranstaltungen`, `Anfahrt`, `Öffnungszeiten`, `Kontakt` (as heading, not in email), `Alle Rechte vorbehalten`, `Rechtliches`, `Sprache`, `Häufig gestellte`, `Unsere Geschichte`, `So finden Sie uns`, `Tisch reservieren` (as heading text), `Impressionen`, `Rufen Sie uns an`, `Jetzt anrufen`, `E-Mail schreiben`, `Datenschutz` (as visible link text — may appear in href which is fine), `Navigation schließen`, `Navigation öffnen`, `Bildergalerie`, `Galerie schließen`, `Vorheriges Bild`, `Nächstes Bild`

**4. German remnant check on IT page — same list as step 3.**

**5. Cross-contamination check — EN strings that should NOT be in IT page:**
- `About Us`, `Our Story`, `Our Menu`, `Business Lunch` (as heading, not in context where it's correct), `Reserve a Table`, `Getting Here`, `Frequently Asked Questions`, `All rights reserved`, `Legal` (as footer heading), `Language` (as footer heading), `Close navigation`, `Open navigation`, `Image gallery`, `Close gallery`, `Previous image`, `Next image`

**6. Section ID integrity — all three files must have identical section IDs:**
Verify each file contains: `id="hero"`, `id="info"`, `id="about"`, `id="menu"`, `id="lunch"`, `id="gallery"`, `id="reservation"`, `id="events"`, `id="location"`, `id="faq"`, `id="contact"`, `id="footer"`

**7. Asset path integrity — verify no asset paths were accidentally modified:**
Confirm `../assets/css/main.css` appears in en/index.html and it/index.html
Confirm `../assets/js/main.js` appears in en/index.html and it/index.html

**8. Footer language switcher — verify each page marks the correct language active:**
- en/index.html: `aria-current="true"` is on the EN link (not DE or IT)
- it/index.html: `aria-current="true"` is on the IT link (not DE or EN)

**9. FAQPage JSON-LD sync check:**
- en/index.html: first FAQ question in JSON-LD matches first `<dt>` text
- it/index.html: first FAQ question in JSON-LD matches first `<dt>` text

If ANY issues are found in steps 3-9, fix them in the EN or IT file immediately. Do not leave known issues for later.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Documents/Ristorante Paganini" && echo "=== Hreflang count per file ===" && echo "DE:" && grep -c 'hreflang=' de/index.html && echo "EN:" && grep -c 'hreflang=' en/index.html && echo "IT:" && grep -c 'hreflang=' it/index.html && echo "=== Canonical ===" && grep 'canonical' de/index.html && grep 'canonical' en/index.html && grep 'canonical' it/index.html && echo "=== German remnants in EN (expect 0) ===" && grep -ic 'Über uns\|Speisekarte\|Öffnungszeiten\|Alle Rechte vorbehalten\|Rechtliches' en/index.html || echo "0 German strings found" && echo "=== German remnants in IT (expect 0) ===" && grep -ic 'Über uns\|Speisekarte\|Öffnungszeiten\|Alle Rechte vorbehalten\|Rechtliches' it/index.html || echo "0 German strings found" && echo "=== Section IDs ===" && grep -c 'id="hero"\|id="info"\|id="about"\|id="menu"\|id="lunch"\|id="gallery"\|id="reservation"\|id="events"\|id="location"\|id="faq"\|id="contact"\|id="footer"' en/index.html && grep -c 'id="hero"\|id="info"\|id="about"\|id="menu"\|id="lunch"\|id="gallery"\|id="reservation"\|id="events"\|id="location"\|id="faq"\|id="contact"\|id="footer"' it/index.html && echo "=== aria-current ===" && grep 'aria-current' en/index.html && grep 'aria-current' it/index.html</automated>
  </verify>
  <acceptance_criteria>
    - All three files have exactly 4 hreflang tags each
    - Each file has self-canonical pointing to its own language directory
    - EN file has 0 matches for German UI strings (Über uns, Speisekarte, Öffnungszeiten, Alle Rechte vorbehalten, Rechtliches)
    - IT file has 0 matches for German UI strings (same list)
    - IT file has 0 matches for English-only UI strings (About Us, Our Story, All rights reserved, Legal as heading)
    - All three files have 12 section IDs (hero, info, about, menu, lunch, gallery, reservation, events, location, faq, contact, footer)
    - EN footer has aria-current on EN link
    - IT footer has aria-current on IT link
    - EN and IT files both contain `../assets/css/main.css` and `../assets/js/main.js`
  </acceptance_criteria>
  <done>All three language files form a complete, valid hreflang reciprocal cluster with zero cross-language contamination, correct self-canonicals, correct language switchers, and intact shared asset paths.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of EN and IT pages</name>
  <files>en/index.html, it/index.html, de/index.html</files>
  <action>
Human visually verifies all three language pages render correctly and language switching works.

What was built: Complete English and Italian localized onepages at /en/index.html and /it/index.html with full hreflang reciprocal cluster across all three language files (DE, EN, IT).

Steps to verify:
1. Open de/index.html in a browser — confirm it looks and works as before (no regressions)
2. Open en/index.html in a browser — verify:
   a. Page loads with correct styling (same layout as DE)
   b. All text is in English (scroll through all 12 sections)
   c. No German strings visible anywhere
   d. H1 tagline is still in Italian ("Dove il gusto incontra la tradizione")
   e. Menu dish names are still in Italian (Tagliatelle al Tartufo, etc.)
   f. Footer language switcher shows EN as active (highlighted)
   g. Click DE and IT links in footer — they navigate to correct pages
   h. Mobile menu works (hamburger opens/closes)
   i. Gallery lightbox works
3. Open it/index.html in a browser — same checks as EN, but:
   a. All text is in Italian with Voi register
   b. Footer shows IT as active
   c. Click DE and EN links in footer — they navigate correctly
4. Spot-check: open browser dev tools on EN page, search for any German text in the source
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — requires manual browser verification"</automated>
  </verify>
  <done>User confirms all three language pages render correctly with proper translations, working language switchers, and no visual regressions. User types "approved" or describes issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cross-page link integrity | Language switcher links must resolve to real files |
| hreflang cluster integrity | All pages must have reciprocal tags — missing tags cause Google to ignore entire cluster |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | hreflang reciprocal cluster | mitigate | Automated grep validation confirms all 4 hreflang tags exist on each of the 3 pages (12 total tag checks). Cross-validation task specifically targets this. |
| T-05-06 | Information Disclosure | Leftover German strings in EN/IT | mitigate | Systematic grep for 15+ German UI strings on each translated file. Any match triggers immediate fix before plan completion. |
</threat_model>

<verification>
1. Hreflang tag count: each file has exactly 4 hreflang tags
2. Self-canonical: each file's canonical points to its own /lang/ URL
3. German remnant scan: 0 German UI strings in en/index.html and it/index.html
4. Cross-contamination: IT file has no English-only strings
5. Section IDs: all 12 IDs present in each file
6. Language switcher: correct aria-current on each file
7. Visual verification: human confirms pages render correctly
</verification>

<success_criteria>
All three language pages (de/index.html, en/index.html, it/index.html) form a complete, valid hreflang reciprocal cluster. Cross-validation confirms zero German remnants in EN/IT pages, zero cross-language contamination, correct self-canonicals, intact section IDs, working language switchers, and preserved shared asset paths. Human visual verification confirms correct rendering and navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-english-and-italian-onepages/05-03-SUMMARY.md`
</output>
