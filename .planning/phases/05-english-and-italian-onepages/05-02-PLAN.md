---
phase: 05-english-and-italian-onepages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - it/index.html
autonomous: true
requirements:
  - LANG-03
user_setup: []

must_haves:
  truths:
    - "Italian visitor sees fully localized content with no German strings on it/index.html"
    - "it/index.html has self-canonical pointing to /it/ URL"
    - "it/index.html has all 4 hreflang tags (de, en, it, x-default)"
    - "og:locale is it_IT and all OG/Twitter meta is in Italian"
    - "JSON-LD Restaurant description and inLanguage are in Italian"
    - "FAQPage JSON-LD has all 7 Q&A pairs translated to Italian matching visible FAQ text"
    - "Footer language switcher has aria-current on IT link"
    - "All aria-label attributes are translated to Italian"
    - "Italian copy uses Voi register throughout"
  artifacts:
    - path: "it/index.html"
      provides: "Complete Italian localized onepage"
      min_lines: 700
  key_links:
    - from: "it/index.html"
      to: "../assets/css/main.css"
      via: "link rel stylesheet"
      pattern: "assets/css/main\\.css"
    - from: "it/index.html"
      to: "../assets/js/main.js"
      via: "script defer"
      pattern: "assets/js/main\\.js"
---

<objective>
Create the complete Italian localized onepage at /it/index.html by copying de/index.html and systematically replacing every German string with the Italian equivalent per the translation reference in 05-RESEARCH.md.

Purpose: Delivers requirement LANG-03 (Italian version fully localized) and completes the second of three files needed for hreflang reciprocal cluster.
Output: it/index.html — a fully functional Italian restaurant page sharing all CSS/JS/image assets with the German version.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-english-and-italian-onepages/05-CONTEXT.md
@.planning/phases/05-english-and-italian-onepages/05-RESEARCH.md
@de/index.html

<interfaces>
<!-- Source file: de/index.html — executor copies this and translates all German strings -->
<!-- Shared assets linked via relative paths (../assets/) — NO changes to paths needed -->
<!-- Section IDs are language-neutral: #hero, #info, #about, #menu, #lunch, #gallery, #reservation, #events, #location, #faq, #contact, #footer — do NOT translate -->
<!-- JS hooks: id="site-header", id="menu-toggle", id="mobile-menu" — do NOT translate -->
<!-- Class names and data attributes — do NOT translate -->
<!-- Italian dish names (Tagliatelle al Tartufo, etc.) — do NOT translate, only translate description text -->
<!-- H1 tagline "Dove il gusto incontra la tradizione" — do NOT translate (brand signature, D-10) -->
<!-- Italian copy uses "Voi" (semi-formal plural) register throughout — D-02 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create it/index.html with full Italian localization</name>
  <files>it/index.html</files>
  <read_first>
    - de/index.html (source template — read entire file to understand structure)
    - .planning/phases/05-english-and-italian-onepages/05-RESEARCH.md (complete translation reference for every string)
    - .planning/phases/05-english-and-italian-onepages/05-CONTEXT.md (locked decisions D-01 through D-16, especially D-02 for Voi register)
  </read_first>
  <action>
Create directory `it/` if it does not exist. Copy `de/index.html` to `it/index.html`. Then systematically replace every German string with the Italian equivalent. Work through the file in this exact order:

**REGISTER NOTE (D-02):** All Italian body copy and CTAs use "Voi" (semi-formal plural) register. Second-person address uses Voi forms (e.g., "prenotate", "scoprite", "godetevi"), NOT Lei or tu. This gives a warmer, more hospitable tone reflecting southern Italian tradition.

**1. HTML element:**
- Change `lang="de"` to `lang="it"` on the `<html>` tag

**2. Head metadata (per D-06, D-07, D-08, D-15):**
- `<title>`: `Ristorante Paganini | Ristorante Italiano Lipsia`
- `<meta name="description">`: `Scoprite la cucina italiana autentica nel cuore di Lipsia. Ristorante Paganini — a conduzione familiare, con passione per la qualità e l'ospitalità da generazioni. Prenotate un tavolo: +49 341 XXXXXXXX`
- `<link rel="canonical">`: change `/de/` to `/it/` in the href
- `og:locale`: change `de_DE` to `it_IT`
- `og:title`: `Ristorante Paganini | Ristorante Italiano Lipsia`
- `og:description`: same as meta description above
- `og:url`: change `/de/` to `/it/`
- `twitter:title`: `Ristorante Paganini | Ristorante Italiano Lipsia`
- `twitter:description`: same as meta description above
- hreflang block: keep ALL 4 tags UNCHANGED (they already point to de, en, it, x-default correctly)

**3. JSON-LD Restaurant/LocalBusiness block (per D-09):**
- `"description"`: `"Autentico ristorante italiano nel centro di Lipsia. A conduzione familiare da generazioni."`
- `"inLanguage"`: `"it"`
- `"url"`: change `/de/` to `/it/`
- All other fields (name, telephone, address, geo, openingHoursSpecification, servesCuisine, priceRange, image) stay IDENTICAL

**4. JSON-LD FAQPage block (per D-04, D-05):**
Replace all 7 Q&A pairs with Italian translations. Each `"name"` and `"acceptedAnswer"."text"` must be translated. Use the exact text from the 05-RESEARCH.md "JSON-LD FAQPage block" section:
- FAQ 1: "Devo prenotare?" / "Vi consigliamo di prenotare, soprattutto nei fine settimana e nei giorni festivi..."
- FAQ 2: "Offrite un pranzo di lavoro?" / "Sì, offriamo un menu di pranzo di lavoro variabile dal lunedì al venerdì dalle 11:30 alle 14:30..."
- FAQ 3: "Dove posso parcheggiare?" / "Nelle immediate vicinanze trovate parcheggi sulla Große Fleischergasse..."
- FAQ 4: "Avete piatti vegetariani o vegani?" / "Certo. Il nostro menù offre una selezione di piatti vegetariani..."
- FAQ 5: "Avete una terrazza?" / "Sì, il nostro ristorante dispone di una piacevole terrazza..."
- FAQ 6: "Quali metodi di pagamento accettate?" / "Accettiamo contanti e tutte le principali carte di credito..."
- FAQ 7: "I cani sono benvenuti?" / "Sì, i cani ben educati sono i benvenuti..."

**5. Header navigation (desktop nav):**
- `Start` → `Home`
- `Über uns` → `Chi Siamo`
- `Speisekarte` → `Menu`
- `Mittagstisch` → `Pranzo di Lavoro`
- `Reservierung` → `Prenotazioni`
- `Kontakt` → `Contatti`

**6. Mobile menu:**
- `aria-label="Navigation öffnen"` → `aria-label="Apri navigazione"`
- `aria-label="Navigation schließen"` → `aria-label="Chiudi navigazione"`
- `Schließen` (close button text) → `Chiudi`
- `aria-label="Navigation"` on mobile menu → `aria-label="Navigazione"`
- All mobile nav link texts: same translations as desktop nav, plus:
  - `Öffnungszeiten` → `Orari`
  - `Galerie` → `Galleria`
  - `Veranstaltungen` → `Eventi`
  - `Anfahrt` → `Come Raggiungerci`
  - `FAQ` → `FAQ` (unchanged)

**7. Body sections — translate all visible text per 05-RESEARCH.md translation reference:**

Section by section:
- **Hero (#hero):** Keep H1 "Dove il gusto incontra la tradizione" UNCHANGED (D-10). Keep "Ristorante Paganini" UNCHANGED (D-11). Translate hero img alt to `Elegante ristorante italiano Ristorante Paganini a Lipsia`. Trust cues: `A conduzione familiare da oltre 30 anni`, `Nel cuore del centro di Lipsia`, `Lun–Sab 11:30–22:00 · +49 341 XXXXXXXX`. CTAs: `Prenota un Tavolo` (primary), `Menu` (secondary).
- **Info bar (#info):** Labels: Orari, Indirizzo, Telefono, Pranzo di Lavoro. Day abbreviations: Lun, Ven, Sab, Dom. CTA: `Prenota un Tavolo`. Terrace: `Terrazza aperta (stagionale)`. Lunch hours: `Lun–Ven 11:30–14:30`.
- **About (#about):** H2 `La Nostra Storia`. Translate all 3 paragraphs per research reference using Voi register.
- **Menu (#menu):** H2 `Il Nostro Menu`. Translate intro para, category heading `Wein`→`Vino`. Translate dish descriptions ONLY (NOT dish names — they are already Italian). CTAs: `Scarica il Menu` and `Menu Completo`. Keep PDF href unchanged.
- **Business Lunch (#lunch):** H2 `Pranzo di Lavoro`. Translate intro, category names: Vorspeise→Antipasto, Hauptgericht→Secondo, Pasta→Pasta, Dessert→Dessert. Translate dish descriptions. Holiday note: `Si prega di notare: il servizio pranzo può variare nei giorni festivi.` CTA: `Scarica il Menu del Pranzo`. Keep PDF href unchanged.
- **Gallery (#gallery):** H2 `Galleria`. Translate all 6 image alt texts and captions per research reference.
- **Reservation (#reservation):** H2 `Prenota un Tavolo`. Translate intro: `Assicuratevi il vostro tavolo al Ristorante Paganini. Vi aspettiamo con piacere.` Widget text: `Prenotate direttamente qui il vostro tavolo. In alternativa: +49 341 XXXXXXXX`. Phone CTA: `Chiamateci`.
- **Events (#events):** H2 `Gruppi &amp; Eventi`. Translate both paragraphs using Voi register. CTAs: `Chiamate Ora` and `Scrivete un'Email`.
- **Location (#location):** H2 `Come Raggiungerci`. `Deutschland`→`Germania`. Parking label `Parcheggio:`. Parking items per research reference. Map CTA: `Visualizza su Google Maps`. Map image alt translated.
- **FAQ (#faq):** H2 `Domande Frequenti`. Translate all 7 dt/dd pairs — text MUST exactly match the FAQPage JSON-LD in the head.
- **Contact (#contact):** H2 `Contatti`. Sub-heading `Contattateci`. Labels: Telefono, Email, Indirizzo. `Orari di Apertura`. Day names: Lunedì–Venerdì, Sabato, Domenica. Instagram: `Seguiteci su Instagram`.

**8. Lightbox modal (after main):**
- `aria-label="Bildergalerie"` → `aria-label="Galleria immagini"`
- Close button `aria-label="Galerie schließen"` → `aria-label="Chiudi galleria"`
- Prev button `aria-label="Vorheriges Bild"` → `aria-label="Immagine precedente"`
- Next button `aria-label="Nächstes Bild"` → `aria-label="Immagine successiva"`

**9. Footer (#footer):**
- `Rechtliches` → `Note Legali`
- `Impressum` → `Note Legali` (link text), href changes to `/it/impressum.html`
- `Datenschutz` → `Privacy` (link text), href changes to `/it/datenschutz.html`
- Legal link hrefs: `/de/impressum.html` → `/it/impressum.html`, `/de/datenschutz.html` → `/it/datenschutz.html`
- `Sprache` → `Lingua`
- Language switcher: move `aria-current="true"` and active styling from DE link to IT link. DE and EN links get plain `color: var(--color-text-inverse)` style. IT link gets `aria-current="true" style="color: var(--color-accent-light); font-weight: var(--font-weight-bold);"`
- Copyright: `Alle Rechte vorbehalten.` → `Tutti i diritti riservati.`

**10. DO NOT change:**
- Any section `id` attribute (they are language-neutral)
- Any CSS class name or data attribute
- Any image `src`, `srcset`, `width`, `height` path
- Any `../assets/` relative path
- The `<!-- EDIT: -->`, `<!-- PLACEHOLDER: -->`, `<!-- SYNC: -->` comment markers (keep in English)
- The H1 Italian tagline
- Italian dish names in menu cards
- PDF file hrefs
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Documents/Ristorante Paganini" && test -f it/index.html && echo "FILE EXISTS" && grep -c 'lang="it"' it/index.html && grep -c 'hreflang="de"' it/index.html && grep -c 'hreflang="en"' it/index.html && grep -c 'hreflang="it"' it/index.html && grep -c 'og:locale.*it_IT' it/index.html && grep -c 'Dove il gusto' it/index.html && echo "--- German string check (expect 0) ---" && grep -c 'Italienisches Restaurant' it/index.html || true && grep -c 'Über uns' it/index.html || true && grep -c 'Speisekarte' it/index.html || true && grep -c 'Alle Rechte vorbehalten' it/index.html || true</automated>
  </verify>
  <acceptance_criteria>
    - it/index.html exists and is >700 lines
    - `<html lang="it">` appears exactly once
    - `<title>` contains "Ristorante Italiano Lipsia" (not "Italienisches")
    - `<link rel="canonical" href="https://www.example.com/it/">` present
    - All 4 hreflang tags present (de, en, it, x-default)
    - `og:locale` content is `it_IT`
    - `og:url` contains `/it/`
    - JSON-LD contains `"inLanguage": "it"`
    - H1 contains "Dove il gusto incontra la tradizione" (unchanged)
    - Zero occurrences of "Italienisches Restaurant" in the file
    - Zero occurrences of "Über uns" in the file
    - Zero occurrences of "Speisekarte" in the file (except possibly in comments referencing the PDF filename)
    - Zero occurrences of "Alle Rechte vorbehalten" in the file
    - Zero occurrences of "Öffnungszeiten" in the file
    - Footer contains `aria-current="true"` on the IT link
    - Footer legal links point to `/it/impressum.html` and `/it/datenschutz.html`
    - FAQ section contains "Devo prenotare?" (first FAQ question in Italian)
    - IT copy uses Voi register: file contains "prenotate" or "scoprite" or "godetevi" (Voi verb forms)
    - All 6 gallery image alt texts are in Italian
    - Section IDs (#hero, #info, #about, etc.) unchanged from DE source
    - All `../assets/` paths unchanged from DE source
  </acceptance_criteria>
  <done>it/index.html is a complete, fully-localized Italian restaurant page using Voi register throughout, with zero German strings in visible content, correct self-canonical, correct hreflang cluster, Italian OG/Twitter meta, Italian JSON-LD, Italian FAQPage schema matching visible FAQ text, and correct footer language switcher with IT active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML output | No user input processed; static content only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | it/index.html meta tags | accept | No PII in meta tags; phone number is intentionally public business info. Placeholder domain (example.com) prevents accidental exposure before launch. |
| T-05-04 | Tampering | hreflang cluster | accept | Static files served from web server; no dynamic content to tamper with. Hreflang integrity verified by cross-page grep checks. |
</threat_model>

<verification>
1. `test -f it/index.html` returns success
2. `grep -c 'lang="de"' it/index.html` returns 0 (no German lang attribute)
3. `grep -c 'lang="it"' it/index.html` returns at least 1
4. `grep -c 'hreflang=' it/index.html` returns 4 (de, en, it, x-default)
5. `grep -c 'canonical.*\/it\/' it/index.html` returns 1
6. `grep -c 'it_IT' it/index.html` returns 1
7. `grep 'Dove il gusto' it/index.html` returns the H1 line (brand signature preserved)
8. No German UI strings remain (spot-check: Über uns, Speisekarte, Öffnungszeiten, Reservierung, Kontakt)
9. Voi register present: `grep -c 'prenotate\|scoprite\|godetevi\|contattateci' it/index.html` returns >0
</verification>

<success_criteria>
it/index.html exists as a complete Italian localized page with:
- Correct `lang="it"`, self-canonical to /it/, og:locale it_IT
- Complete hreflang reciprocal cluster (4 tags)
- All 12 sections translated with Voi register (D-02)
- FAQPage JSON-LD matches visible FAQ text
- Footer language switcher marks IT as active
- Zero German strings in visible content or meta tags
- All shared asset paths (CSS, JS, images, fonts) work correctly via ../assets/
</success_criteria>

<output>
After completion, create `.planning/phases/05-english-and-italian-onepages/05-02-SUMMARY.md`
</output>
