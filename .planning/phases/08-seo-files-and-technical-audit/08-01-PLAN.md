---
phase: 08-seo-files-and-technical-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sitemap.xml
  - robots.txt
autonomous: true
requirements:
  - SEO-06
  - SEO-07

must_haves:
  truths:
    - "sitemap.xml exists at site root and lists exactly 3 onepage URLs with hreflang alternate entries"
    - "robots.txt exists at site root, allows all content pages, disallows root redirect, and references sitemap.xml"
    - "All URLs in sitemap.xml use the same https://www.example.com placeholder domain as the HTML files"
  artifacts:
    - path: "sitemap.xml"
      provides: "Multilingual sitemap with hreflang alternates"
      contains: "xmlns:xhtml"
    - path: "robots.txt"
      provides: "Crawler directives with sitemap reference"
      contains: "Sitemap:"
  key_links:
    - from: "robots.txt"
      to: "sitemap.xml"
      via: "Sitemap: directive"
      pattern: "Sitemap:.*sitemap\\.xml"
    - from: "sitemap.xml"
      to: "de/index.html, en/index.html, it/index.html"
      via: "loc URLs matching canonical URLs in HTML"
      pattern: "example\\.com/de/"
---

<objective>
Create sitemap.xml and robots.txt at the site root, completing the SEO infrastructure for the fully-built multilingual restaurant website.

Purpose: These two files are the final pieces Google needs to discover and correctly index all three language versions of the site. The sitemap reinforces hreflang signals already present in HTML, and robots.txt prevents indexing of the root redirect utility page.

Output: sitemap.xml with 3 URL entries and hreflang alternates, robots.txt with minimal directives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-seo-files-and-technical-audit/08-CONTEXT.md
@.planning/phases/08-seo-files-and-technical-audit/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sitemap.xml with multilingual hreflang entries</name>
  <files>sitemap.xml</files>
  <read_first>
    - de/index.html (extract the canonical URL format to confirm trailing-slash form and placeholder domain)
    - .planning/phases/08-seo-files-and-technical-audit/08-RESEARCH.md (verified sitemap XML structure in "Pattern 1" and "Code Examples" sections)
  </read_first>
  <action>
Create sitemap.xml at the project root with the following exact content structure (per D-01, D-02, D-03, D-04, D-05):

1. XML declaration: the standard xml version 1.0 encoding UTF-8 processing instruction
2. Comment line immediately after: REPLACE example.com WITH REAL DOMAIN BEFORE LAUNCH (as XML comment)
3. Opening urlset with TWO namespace declarations:
   - xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
   - xmlns:xhtml="http://www.w3.org/1999/xhtml"

4. Exactly 3 url blocks for DE, EN, IT (per D-01 -- no legal pages, no root redirect). Each url block contains:
   - loc with the page absolute URL using https://www.example.com placeholder domain (per D-04)
   - lastmod set to 2026-04-10 (W3C date format)
   - 4 xhtml:link elements (per D-02, D-03):
     - rel="alternate" hreflang="de" href="https://www.example.com/de/"
     - rel="alternate" hreflang="en" href="https://www.example.com/en/"
     - rel="alternate" hreflang="it" href="https://www.example.com/it/"
     - rel="alternate" hreflang="x-default" href="https://www.example.com/de/"

CRITICAL: The loc URLs MUST use trailing-slash directory form (/de/, /en/, /it/) -- match the canonical URLs already in the HTML files exactly. Do NOT use /de/index.html form. Read the canonical link from de/index.html to confirm the exact format before writing.

Do NOT include changefreq or priority elements -- both are optional and largely ignored by Google.

5. Closing urlset

The exact XML content to write is provided in 08-RESEARCH.md section "Complete sitemap.xml" -- use that verbatim.
  </action>
  <verify>
    <automated>node -e "var fs=require('fs');var xml=fs.readFileSync('sitemap.xml','utf8');var c=[xml.includes('xmlns:xhtml'),xml.includes('REPLACE example.com'),xml.split('&lt;url&gt;').length-1===3||xml.split(String.fromCharCode(60)+'url'+String.fromCharCode(62)).length-1===3,xml.includes('x-default'),xml.includes('hreflang'),!xml.includes('changefreq')];c.forEach(function(v,i){console.log((v?'PASS':'FAIL')+' check '+i)});if(!c.every(Boolean))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - sitemap.xml exists at project root (same level as index.html)
    - File contains exactly 3 url blocks (one for /de/, one for /en/, one for /it/)
    - Each url block contains 4 xhtml:link elements with hreflang de, en, it, x-default
    - x-default href points to /de/ (German as default)
    - All URLs use https://www.example.com placeholder domain
    - XML comment at top contains "REPLACE example.com WITH REAL DOMAIN BEFORE LAUNCH"
    - xmlns:xhtml="http://www.w3.org/1999/xhtml" namespace declared on urlset
    - No changefreq or priority elements present
    - loc values use trailing-slash form (/de/ not /de/index.html)
  </acceptance_criteria>
  <done>sitemap.xml exists at root with 3 URL entries, each containing 4 xhtml:link hreflang alternates, using the placeholder domain, with the launch-replacement comment</done>
</task>

<task type="auto">
  <name>Task 2: Create robots.txt with crawler directives and sitemap reference</name>
  <files>robots.txt</files>
  <read_first>
    - sitemap.xml (confirm it exists from Task 1 and the filename matches)
    - .planning/phases/08-seo-files-and-technical-audit/08-RESEARCH.md (verified robots.txt structure in "Pattern 2" and "Code Examples" sections)
  </read_first>
  <action>
Create robots.txt at the project root with the following exact content (per D-07, D-08, D-09, D-10, D-04, D-05):

Line 1: Comment -- "Ristorante Paganini -- robots.txt"
Line 2: Comment -- "REPLACE example.com WITH REAL DOMAIN BEFORE LAUNCH" (per D-05)
Line 3: blank line
Line 4: User-agent: *
Line 5: Allow: /
Line 6: Allow: /assets/
Line 7: Disallow: /index.html
Line 8: blank line
Line 9: Sitemap: https://www.example.com/sitemap.xml

Key decisions implemented:
- Per D-07: Only disallow /index.html (the root redirect). This does NOT match /de/index.html -- robots.txt path matching is prefix-based, and /index.html only matches the exact root file.
- Per D-08: Explicit Allow: /assets/ so Google can render pages using CSS/JS/images.
- Per D-09: test.html in assets/css/ is NOT explicitly blocked (it won't be crawled because nothing links to it).
- Per D-10: Sitemap directive at the bottom with absolute URL using placeholder domain.

The exact text content to write is provided in 08-RESEARCH.md section "Complete robots.txt" -- use that verbatim.
  </action>
  <verify>
    <automated>node -e "var fs=require('fs');var txt=fs.readFileSync('robots.txt','utf8');var c=[txt.includes('User-agent: *'),txt.includes('Allow: /'),txt.includes('Allow: /assets/'),txt.includes('Disallow: /index.html'),txt.includes('Sitemap: https://www.example.com/sitemap.xml'),txt.includes('REPLACE example.com'),!txt.includes('Disallow: /de/'),!txt.includes('Disallow: /en/'),!txt.includes('Disallow: /it/')];c.forEach(function(v,i){console.log((v?'PASS':'FAIL')+' check '+i)});if(!c.every(Boolean))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - robots.txt exists at project root (same level as index.html)
    - Contains "User-agent: *"
    - Contains "Allow: /"
    - Contains "Allow: /assets/"
    - Contains "Disallow: /index.html"
    - Contains "Sitemap: https://www.example.com/sitemap.xml"
    - Contains comment "REPLACE example.com WITH REAL DOMAIN BEFORE LAUNCH"
    - Does NOT contain Disallow for /de/, /en/, or /it/ paths
    - Does NOT contain Crawl-delay directive
  </acceptance_criteria>
  <done>robots.txt exists at root with User-agent *, Allow /, Allow /assets/, Disallow /index.html, and Sitemap directive with placeholder domain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Crawler to sitemap/robots | Public files read by any web crawler -- no sensitive data exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | robots.txt Disallow entries | accept | Minimal Disallow list per D-07; only /index.html (non-sensitive redirect page) is listed. No admin paths, no sensitive directories disclosed. |
| T-08-02 | Information Disclosure | sitemap.xml URL listing | accept | Lists only public onepage URLs -- no private or draft content. Legal pages with noindex are excluded per D-01. |
</threat_model>

<verification>
1. sitemap.xml exists at project root and contains valid XML with 3 url blocks
2. robots.txt exists at project root with correct directives
3. Both files use https://www.example.com placeholder domain consistently
4. Both files contain "REPLACE example.com" launch comment
5. sitemap.xml loc URLs match the canonical URLs in existing HTML files (trailing-slash form)
6. robots.txt Disallow: /index.html does not affect language subdirectory pages
</verification>

<success_criteria>
- sitemap.xml passes XML well-formedness check
- robots.txt contains all required directives (User-agent, Allow, Disallow, Sitemap)
- Placeholder domain is consistent across both new files and all existing HTML files
- No content pages are blocked by robots.txt
</success_criteria>

<output>
After completion, create .planning/phases/08-seo-files-and-technical-audit/08-01-SUMMARY.md
</output>
